<Window x:Class="Tema3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tema3"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#FFE0E0E0" LastChildFill="False">
            <StackPanel Orientation="Horizontal" Margin="5,2">
                <Button x:Name="ResetButton"
                        Content="Reset Polynom"
                        Margin="0,0,5,0"
                        Padding="10,4"
                        Click="ResetButton_Click"/>
            </StackPanel>

            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="LAGRANGE INTERPOLATION POLYNOM:" FontWeight="Bold" Margin="5,0"/>
            </StackPanel>
        </DockPanel>

        <Canvas x:Name="DrawingCanvas"
                Grid.Row="1"
                Background="White" />
    </Grid>
</Window>
