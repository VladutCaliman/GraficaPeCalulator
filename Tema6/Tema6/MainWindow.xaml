<Window x:Class="Tema6.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tema6"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#FFE0E0E0" LastChildFill="False">
            <StackPanel Orientation="Horizontal" Margin="5,2">
                <Button x:Name="ResetButton"
                        Content="Resetare Puncte"
                        Margin="0,0,5,0"
                        Padding="10,4"/>
                <TextBlock VerticalAlignment="Center" Margin="10,0" Foreground="#666">
                    Click Stânga: Adaugă Puncte | Click Dreapta: Finalizează | Drag: Mută Puncte
                </TextBlock>
            </StackPanel>
        </DockPanel>

        <Canvas x:Name="DrawingCanvas"
                Grid.Row="1"
                Background="White" />
    </Grid>
</Window>
