<Window x:Class="Tema1.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Tema1"
        mc:Ignorable="d"
        Title="Polygon Drawer" Height="550" Width="1100">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Background="#FFE0E0E0" LastChildFill="False">
            <StackPanel Orientation="Horizontal" Margin="5,2">
                <Button x:Name="ResetButton"
                        Content="Reset Polygon"
                        Margin="0,0,5,0"
                        Padding="10,4" />

                <Button x:Name="RotateButton"
                        Content="Rotate"
                        Margin="0,0,5,0"
                        Padding="10,4" />

                <Button x:Name="ScaleButton"
                        Content="Scale"
                        Margin="0,0,5,0"
                        Padding="10,4" />

                <Button x:Name="TranslateButton"
                        Content="Translate"
                        Margin="0,0,5,0"
                        Padding="10,4" />

                <Button x:Name="SymmetryOriginButton"
                        Content="Symmetry Origin"
                        Margin="0,0,5,0"
                        Padding="10,4" />

                <Button x:Name="SymmetryLineButton"
                        Content="Symmetry Line"
                        Margin="0,0,5,0"
                        Padding="10,4" />

                <TextBlock Text="Reference:"
                           VerticalAlignment="Center"
                           Margin="10,0,5,0" />

                <RadioButton x:Name="OriginRadio"
                             Content="Origin (0,0)"
                             GroupName="ReferenceGroup"
                             IsChecked="True"
                             VerticalAlignment="Center"
                             Margin="0,0,5,0" />

                <RadioButton x:Name="CenterRadio"
                             Content="Polygon center"
                             GroupName="ReferenceGroup"
                             VerticalAlignment="Center" />
                
                <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" Margin="10,0"/>

                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <TextBlock Text="Active Tool:" FontWeight="Bold" Margin="5,0"/>
                    <TextBlock x:Name="StatusLabel" Text="None (Drawing)" Foreground="Blue"/>
                </StackPanel>
            </StackPanel>
        </DockPanel>

        <Canvas x:Name="DrawingCanvas"
                Grid.Row="1"
                Background="White" />
    </Grid>
</Window>
